kind = "web"
previewPath = "/"
title = "Novadent 諾星牙科平台"
version = "1.0.0"
id = "artifacts/novadent-app"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = ["/"]
localPort = 24505

[services.development]
run = "cd /home/runner/workspace/novadent/backend && NODE_ENV=production node dist/src/main.js"

[services.production]
deploy = "autoscale"
build = ["bash", "-c", "cd /home/runner/workspace/novadent && pnpm install && npx vite build --outDir dist && cd /home/runner/workspace/novadent/backend && npx tsc -p tsconfig.json || true"]
run = "cd /home/runner/workspace/novadent/backend && NODE_ENV=production PORT=24505 node dist/src/main.js"

[services.env]
PORT = "24505"
